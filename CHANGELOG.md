# Changelog

## 0.0.2

- Write read-only exclusions before enabling workspace read-only mode, preventing the settings file from being locked before it can be excluded.

## 0.0.1

- Add status bar toggle for workspace read-only mode.
- Add lock, unlock, and toggle commands.
- Preserve and restore previous workspace read-only settings.
